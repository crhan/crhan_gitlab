

= render 'layouts/subnav'

%h2 Owned Projects

%table.table.talbe-striped
  %thead
    %tr
      %th Name
      %th Owner
      %th Action
  %tbody
    = render :partial => 'projects/index', :locals => { :projects => @profile.owns }
