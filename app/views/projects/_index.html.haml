- projects.each do |p|
  %tr
    %td
      = link_to [p] do
        %p
          %strong= p.name
    %td
      %small
        = owner_email(p)
    %td
      - if p.latest_event
        Last activity at
        = p.latest_event_date
    %td= link_to "Remove", [p], :method => :delete, :class => "btn btn-danger pull-right", :confirm => "Are you sure?"
